{% extends '@MasterPeaceUpRead/UserLayout/student.html.twig' %}

{% block page_wrapper %}
    <strong>{{ quiz.title }}</strong>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-info">{{ 'quiz.create.save.button'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}