{% extends '@MasterPeaceUpRead/HomePage/teacher.html.twig' %}

{% block page_wrapper %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">{{ 'quiz.list.title'|trans }}</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <a href="{{ path('quiz_create') }}" class="btn btn-success">{{ 'quiz.create.title'|trans }}</a>
        </div>
    </div>
    <br />
    <div class="row">
        {% for quiz in quizes %}

        {% endfor %}
        <!-- /.row -->
        <!-- /#page-wrapper -->
    </div>
{% endblock %}
