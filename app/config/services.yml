# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:

services:

    masterpeace.form.registration:
        class: MasterPeace\Bundle\UserBundle\Form\RegistrationType
        tags:
            - { name: form.type, alias: masterpeace_user_registration }
